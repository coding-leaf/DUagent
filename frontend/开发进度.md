# 接口对接开发进度 (Task Progress)

- `[x]` **Phase 1: 基础设施建设 (Infrastructure)**
  - `[x]` 安装 `axios` 和 `axios-mock-adapter`。
  - `[x]` 创建 `src/api/client.js`，配置 Axios 实例、JWT Token 请求拦截器、统一错误响应拦截器。
  - `[x]` 搭建 `src/api/mock` 拦截体系，实现 API 文档的 JSON 替代表现（支持随时关闭）。

- `[x]` **Phase 2: 认证与用户模块对接 (Auth & User)**
  - `[x]` 封装 `src/api/services/auth.js`。
  - `[x]` 改造 `Login.jsx`：对接获取验证码、登录、存储 Token 逻辑。
  - `[x]` 改造 `Register.jsx`：对接注册验证码、校验并提交注册。
  - `[x]` 路由层增加简单的 Token 鉴权逻辑（可选/视需要）。

- `[x]` **Phase 3: 学习主流程对接 (Learning & Resource)**
  - `[x]` 封装 `src/api/services/course.js` & `learning.js`。
  - `[x]` 改造 `Dashboard.jsx`（资源库）：从 `/api/v1/courses` 获取实际资源列表与分类。
  - `[x]` 改造 `LearningPath.jsx`（路径规划）：从 `/api/v1/learning-path` 获取动态路径节点。

- `[x]` **Phase 4: 练习与画像模块对接 (Quiz & Profile) (Completed)**
  - `[x]` 封装 `src/api/services/quiz.js` & `profile.js`。
  - `[x]` 改造 `Quiz.jsx`：从 `/api/v1/quiz/questions` 动态加载题目数据。
  - `[x]` 改造 `StudentProfile.jsx` / `LearningEffects.jsx`，加载图表数据。
  - `[x]` 实现 `/api/v1/quiz/submit` 逻辑并将测试结果传递给结果展示页。

- `[x]` **Phase 5: 教师端模块对接 (Teacher Console) (Completed)**
  - `[x]` 封装 `src/api/services/teaching.js`。
  - `[x]` 改造 `TeacherConsole.jsx`，获取班级学生列表与学习统计。
  - `[x]` 改造 `TeacherStudentReport.jsx`，获取特定学生学情报告。
