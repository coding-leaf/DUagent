# EDUagent 联调测试指导

## 1. 目标

本文用于当前 `backend + agent_service + MySQL + Qdrant` 联调阶段的手工测试。

范围：

- 验证 Backend 服务可启动并正常响应前端接口
- 验证 Backend 能通过 HTTP 调用 Agent Service
- 验证异步任务、SSE、Webhook 的主链路
- 提供 Apifox 中可直接录入的测试顺序、接口和预期结果

不包含：

- `agent_service/` 内部 LLM 提示词调优
- 自动化测试脚本修复
- 前端 UI 测试

契约来源：

- `docs/10-client-api/API_前端接口规范.md`
- `docs/20-agent-api/API_Agent内部接口规范.md`
- `docs/backend-agent-integration-reference.md`

## 2. 联调前置条件

### 2.1 服务与依赖

联调前需确保以下组件已经启动：

- MySQL: `127.0.0.1:3306`
- Qdrant: `127.0.0.1:6333`
- Agent Service: `127.0.0.1:8002`
- Backend: `127.0.0.1:8001`

推荐在两个终端分别启动：

```bash
# 终端 1：Agent Service
cd /home/yezisama/workspace/workflow/EDUagent
source .venv/bin/activate
python -m uvicorn agent_service.main:app --host 127.0.0.1 --port 8002 --reload
```

```bash
# 终端 2：Backend
cd /home/yezisama/workspace/workflow/EDUagent/backend
source ../.venv/bin/activate
python -m uvicorn app.main:app --host 127.0.0.1 --port 8001 --reload
```

### 2.2 健康检查

启动完成后先验证基础可用性：

```bash
curl http://127.0.0.1:8001/health
curl http://127.0.0.1:8002/agent/v1/health
```

预期：

- Backend 返回 `{"status":"ok","version":"5.0"}`
- Agent Service 返回 `{"code":200,"message":"success","data":{...}}`
- Agent `data.status = healthy`
- Agent `data.qdrant_connected = true`

## 3. Apifox 使用建议

建议在 Apifox 中创建两个环境：

### 3.1 Backend 环境

- `baseUrl = http://127.0.0.1:8001`
- `student_token =`
- `teacher_token =`
- `course_id =`
- `course_code =`
- `task_id =`
- `conversation_id =`

### 3.2 Agent 环境

- `agentBaseUrl = http://127.0.0.1:8002`

### 3.3 调试原则

- 不要一开始就测全部接口
- 按“同步 JSON → SSE → 异步任务 + Webhook”顺序推进
- 每打通一条链路后，再继续下一条
- 同时观察：
  - Backend 终端日志
  - Agent Service 终端日志
  - Apifox 响应内容
  - MySQL 中任务/业务数据变化

## 4. 推荐联调顺序

建议按下列顺序执行：

1. 健康检查
2. 验证码、注册、登录
3. 老师建课、学生加课
4. `POST /api/v1/profile/refresh`
5. `GET /api/v1/tasks/{task_id}`
6. `POST /api/v1/tutoring/chat`
7. `POST /api/v1/resources/generate`
8. `POST /api/v1/quiz/generate`
9. `POST /api/v1/evaluation/refresh`
10. `POST /api/v1/learning-path/refresh`

## 5. 核心测试用例

---

## 用例 1：Backend 健康检查

### 接口

- `GET /health`

### 请求

- Method: `GET`
- URL: `{{baseUrl}}/health`

### 预期返回

HTTP 状态码：

- `200`

响应体：

```json
{
  "status": "ok",
  "version": "5.0"
}
```

### 判定标准

- 服务已启动
- FastAPI 路由加载成功

---

## 用例 2：Agent Service 健康检查

### 接口

- `GET /agent/v1/health`

### 请求

- Method: `GET`
- URL: `{{agentBaseUrl}}/agent/v1/health`

### 预期返回

HTTP 状态码：

- `200`

响应体关键字段：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "status": "healthy",
    "qdrant_connected": true
  }
}
```

### 判定标准

- Agent Service 已启动
- Qdrant 已连接

---

## 用例 3：获取验证码

### 接口

- `GET /api/v1/auth/captcha`

### 请求

- Method: `GET`
- URL: `{{baseUrl}}/api/v1/auth/captcha`

### 预期返回

HTTP 状态码：

- `200`

响应体关键字段：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "captcha_token": "captcha_xxx",
    "captcha_question": "3 + 5 = ?"
  }
}
```

### Apifox 操作建议

- 手工计算 `captcha_question` 的结果
- 将 `captcha_token` 复制到后续注册或登录请求

---

## 用例 4：注册教师账号

### 接口

- `POST /api/v1/auth/register`

### 请求体示例

```json
{
  "registration_code": "teacher",
  "email": "teacher1@test.com",
  "password": "Abc12345",
  "username": "teacher1",
  "captcha_token": "captcha_xxx",
  "captcha_code": "8"
}
```

### 预期返回

HTTP 状态码：

- `201`

响应体关键字段：

```json
{
  "code": 201,
  "message": "created",
  "data": {
    "user_id": "xxx",
    "email": "teacher1@test.com",
    "username": "teacher1"
  }
}
```

### 常见失败

- `40001`: 注册码无效
- `40002`: 验证码错误或过期
- `40900`: 邮箱已注册
- `40901`: 用户名已被占用

---

## 用例 5：注册学生账号

### 接口

- `POST /api/v1/auth/register`

### 请求体示例

```json
{
  "registration_code": "student",
  "email": "student1@test.com",
  "password": "Abc12345",
  "username": "student1",
  "captcha_token": "captcha_xxx",
  "captcha_code": "8"
}
```

### 预期返回

HTTP 状态码：

- `201`

响应体关键字段：

```json
{
  "code": 201,
  "message": "created",
  "data": {
    "user_id": "xxx",
    "email": "student1@test.com",
    "username": "student1"
  }
}
```

---

## 用例 6：教师登录

### 接口

- `POST /api/v1/auth/login`

### 请求体示例

```json
{
  "email": "teacher1@test.com",
  "password": "Abc12345",
  "captcha_token": "captcha_xxx",
  "captcha_code": "8"
}
```

### 预期返回

HTTP 状态码：

- `200`

响应体关键字段：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "token": "jwt_xxx",
    "expires_in": 604800,
    "user": {
      "id": "xxx",
      "email": "teacher1@test.com",
      "username": "teacher1",
      "role": "teacher"
    }
  }
}
```

### Apifox 操作建议

- 将 `data.token` 保存到环境变量 `teacher_token`

---

## 用例 7：学生登录

### 接口

- `POST /api/v1/auth/login`

### 预期返回

与教师登录一致，但 `user.role = student`

### Apifox 操作建议

- 将 `data.token` 保存到环境变量 `student_token`

---

## 用例 8：教师创建课程

### 接口

- `POST /api/v1/courses`

### Header

- `Authorization: Bearer {{teacher_token}}`

### 请求体示例

```json
{
  "name": "联调测试课"
}
```

### 预期返回

HTTP 状态码：

- `201`

响应体关键字段：

```json
{
  "code": 201,
  "message": "created",
  "data": {
    "id": "course_xxx",
    "name": "联调测试课",
    "course_code": "ABC123"
  }
}
```

### Apifox 操作建议

- 保存 `data.id -> course_id`
- 保存 `data.course_code -> course_code`

---

## 用例 9：学生加入课程

### 接口

- `POST /api/v1/courses/join`

### Header

- `Authorization: Bearer {{student_token}}`

### 请求体示例

```json
{
  "course_code": "{{course_code}}"
}
```

### 预期返回

HTTP 状态码：

- `200`

响应体关键字段：

```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

### 常见失败

- `404`: 课程码不存在
- `409`: 已重复加入

---

## 用例 10：画像刷新联调

### 目标

这是第一条必须优先打通的 Agent 联调链路。

### Backend 接口

- `POST /api/v1/profile/refresh?course_id={{course_id}}`

### Agent 对应接口

- `POST /agent/v1/profile/generate`

### Header

- `Authorization: Bearer {{student_token}}`

### 请求

- Method: `POST`
- URL: `{{baseUrl}}/api/v1/profile/refresh?course_id={{course_id}}`

### 预期返回

HTTP 状态码：

- `202`

响应体关键字段：

```json
{
  "code": 202,
  "message": "accepted",
  "data": {
    "task_id": "task_xxx"
  }
}
```

### 额外观察点

- Backend 日志应显示调用 Agent Service
- Agent 日志应显示收到 `/agent/v1/profile/generate`
- MySQL `async_tasks` 应新增 `profile_refresh`

### Apifox 操作建议

- 保存 `data.task_id -> task_id`

---

## 用例 11：查询任务状态

### 接口

- `GET /api/v1/tasks/{{task_id}}`

### Header

- `Authorization: Bearer {{student_token}}`

### 预期返回

HTTP 状态码：

- `200`

响应体关键字段：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "task_id": "task_xxx",
    "task_type": "profile_refresh",
    "status": "processing",
    "progress": 0,
    "error_code": null,
    "error_message": null
  }
}
```

### 允许的状态

- `processing`
- `completed`
- `failed`

### 判定标准

- `task_type` 必须正确
- 状态必须在契约定义内

---

## 用例 12：SSE 辅导联调

### Backend 接口

- `POST /api/v1/tutoring/chat`

### Agent 对应接口

- `POST /agent/v1/tutoring/chat`

### Header

- `Authorization: Bearer {{student_token}}`
- `Content-Type: application/json`

### 请求体示例

```json
{
  "message": "请帮我解释牛顿第二定律",
  "scope": "global"
}
```

### 预期返回

HTTP 状态码：

- `200`

响应头：

- `Content-Type: text/event-stream`

响应内容应包含以下 SSE 事件之一或多条：

```text
data: {"type":"chunk","content":"..."}
data: {"type":"diagram","data":"..."}
data: {"type":"done", ...}
```

### 判定标准

- Backend 能代理 SSE
- 响应不是普通 JSON
- 最终能看到 `done` 事件

### 额外说明

- 若 `scope=course`，则必须传 `course_id`
- 否则预期返回 `400`

---

## 用例 13：资源生成异步联调

### Backend 接口

- `POST /api/v1/resources/generate`

### Agent 对应接口

- `POST /agent/v1/resources/generate`

### Header

- `Authorization: Bearer {{teacher_token}}`

### 请求体示例

```json
{
  "course_id": "{{course_id}}"
}
```

### 预期返回

HTTP 状态码：

- `202`

响应体关键字段：

```json
{
  "code": 202,
  "message": "accepted",
  "data": {
    "task_id": "task_xxx"
  }
}
```

### 后续验证

1. 查询任务状态
2. 观察 Agent 是否回调 Backend `/api/v1/webhooks/agent`
3. 查询资源列表确认是否落库

### 当前已知风险

当前实现存在已知问题：

- Webhook `completed` 后可能未将任务状态更新为 `completed`
- 因此可能出现“资源已写入但任务仍显示 processing”

---

## 用例 14：题目生成异步联调

### Backend 接口

- `POST /api/v1/quiz/generate`

### Agent 对应接口

- `POST /agent/v1/assessment/generate-questions`

### Header

- `Authorization: Bearer {{student_token}}`

### 请求体示例

```json
{
  "course_id": "{{course_id}}",
  "count": 3,
  "personalized": true
}
```

### 预期返回

- HTTP `202`
- 返回 `task_id`

### 后续验证

- `GET /api/v1/tasks/{{task_id}}`
- 题目列表接口是否能查到新增题目

---

## 用例 15：学习效果评估刷新联调

### Backend 接口

- `POST /api/v1/evaluation/refresh?course_id={{course_id}}`

### Agent 对应接口

- `POST /agent/v1/evaluation/generate`

### Header

- `Authorization: Bearer {{student_token}}`

### 预期返回

- HTTP `202`
- 返回 `task_id`

### 后续验证

- 任务状态可查询
- `GET /api/v1/evaluation?course_id={{course_id}}` 可查到最新结果

---

## 用例 16：学习路径刷新联调

### Backend 接口

- `POST /api/v1/learning-path/refresh?course_id={{course_id}}`

### Agent 对应接口

- `POST /agent/v1/learning-path/generate`

### Header

- `Authorization: Bearer {{student_token}}`

### 预期返回

- HTTP `202`
- 返回 `task_id`

### 后续验证

- 任务状态可查询
- `GET /api/v1/learning-path?course_id={{course_id}}` 可查到最新结果

## 6. 建议的 Apifox 目录结构

建议按以下目录建集合：

```text
Backend Health
Auth
Courses
Tasks
Profile
Tutoring
Resources
Quiz
Evaluation
Learning Path
Agent Health
```

各目录建议包含：

- `Auth`
  - 获取验证码
  - 注册教师
  - 注册学生
  - 教师登录
  - 学生登录
- `Courses`
  - 教师创建课程
  - 学生加入课程
  - 查询课程列表
- `Tasks`
  - 查询任务状态
- `Profile`
  - 刷新画像
- `Tutoring`
  - 发起 SSE 对话
- `Resources`
  - 发起资源生成
  - 查询资源列表
- `Quiz`
  - 发起题目生成
  - 查询题目列表
- `Evaluation`
  - 刷新评估
  - 获取评估
- `Learning Path`
  - 刷新学习路径
  - 获取学习路径

## 7. 常见问题定位

### 7.1 Backend 能启动但接口 500

优先检查：

- MySQL 用户名、密码、数据库名是否正确
- 表是否自动创建成功
- 终端 traceback 是否来自 SQLAlchemy、JWT、Pydantic

### 7.2 Backend 接口 202，但任务一直不完成

优先检查：

- Agent Service 日志是否收到请求
- Agent 是否回调 `/api/v1/webhooks/agent`
- `task_id` 是否一致
- 当前 webhook 是否命中了已知状态更新问题

### 7.3 `tutoring/chat` 返回不是 SSE

优先检查：

- 请求是否正确使用 POST
- `scope=course` 时是否传了 `course_id`
- Agent Service 是否返回 4xx/5xx
- Backend 是否在代理流前提前报错

### 7.4 `resources/generate` 返回 202，但无资源落库

优先检查：

- Agent 是否真的执行完成
- Webhook 是否成功到达 Backend
- `req.result.resources` 是否存在
- Backend webhook 是否校验失败后提前返回

## 8. 当前已知限制

- `POST /api/v1/webhooks/agent` 当前无额外鉴权，默认通过内网访问控制
- Webhook `completed` 状态更新存在已知缺陷，任务状态可能停留在 `processing`
- `GET /api/v1/learning-path/nodes/{id}/resources` 当前是 stub
- `POST /api/v1/quiz/submit` 诊断结果当前为硬编码文本

## 9. 最小闭环建议

如果时间有限，至少完成以下最小闭环：

1. `GET /health`
2. `GET /api/v1/auth/captcha`
3. 注册教师
4. 注册学生
5. 教师登录
6. 学生登录
7. 教师创建课程
8. 学生加入课程
9. `POST /api/v1/profile/refresh`
10. `GET /api/v1/tasks/{task_id}`

这条链路打通后，说明以下关键能力已基本可用：

- Backend 鉴权
- MySQL 读写
- 课程权限校验
- Backend 调 Agent Service
- 异步任务创建与查询

