# EDUagent 联调验收指导

## 1. 文档目标

本文不是接口清单，而是一份可重复执行的联调验收手册，供人或 AI 在 `backend + agent_service + MySQL + Qdrant` 环境中按统一流程完成验证、记录结论并定位失败点。

本文目标：

- 固化当前推荐的启动顺序、验收顺序和判定标准
- 避免只看 HTTP `200/202` 就误判“链路通过”
- 区分“真实通过”“降级可用”“未通过”
- 为后续 AI 提供跨窗口可复用的执行步骤

本文不负责：

- 修改接口契约
- 调整 LLM 提示词
- 设计新功能
- 代替自动化测试

契约来源：

- `docs/10-client-api/Client-API.openapi.json`
- `docs/10-client-api/API_前端接口规范.md`
- `docs/20-agent-api/Agent-Service.openapi.json`
- `docs/20-agent-api/API_Agent内部接口规范.md`
- `docs/backend-agent-integration-reference.md`

## 2. 当前系统事实

执行联调前，先接受以下事实，不要凭猜测定义预期：

- Backend 负责鉴权、课程权限、SQL 持久化、任务状态、Webhook 落库。
- Agent Service 不直接读写 Backend MySQL，只通过 HTTP 与 Backend 交互。
- Backend 不直接访问 Qdrant。
- `profile/refresh`、`evaluation/refresh`、`learning-path/refresh` 当前是进程内 `asyncio.create_task` 真异步，不是持久化 worker。
- `resources/generate` 是异步 + Webhook 闭环链路。
- `tutoring/chat` 是当前最关键的真实 RAG 验收链路。
- `learning-path/refresh` 不是 RAG 型接口；它依赖 Backend 传入的 `knowledge_graph + profile + evaluation`。
- `learning-path/refresh` 若缺少 `CourseKnowledgeGraph`，返回空 `nodes/edges` 是合理降级，不应误判为故障。
- Agent 侧 embeddings 400 已修复；课程 `758aeff588e84044` 的知识已灌入 Qdrant，已验证可命中。

## 3. 通过标准

联调时只允许使用以下三类结论：

- `真实通过`
  - HTTP 返回符合契约
  - Agent 实际参与
  - 任务终态正确
  - SQL/业务数据真实落库
  - 若该接口是 RAG 型链路，还必须看到检索命中证据
- `降级可用`
  - 接口主链未崩
  - 结果符合当前降级语义
  - 但未达到完整业务效果
  - 例如 `learning-path/refresh` 在无 `CourseKnowledgeGraph` 时返回空路径
- `未通过`
  - 契约不符
  - task 卡死
  - Agent 未被调用
  - Webhook 未闭环
  - 应有 RAG 而没有命中，且不是设计预期

禁止使用模糊结论，例如“差不多可以”“基本通过”“看起来没问题”。

## 4. 联调前置条件

### 4.1 服务启动顺序

推荐顺序：

1. MySQL
2. Qdrant
3. Agent Service
4. Backend

MySQL：

```bash
docker start eduagent-mysql
```

Qdrant：

```bash
docker start eduagent-qdrant
```

若容器未创建，可首次使用：

```bash
docker run -d --name eduagent-mysql -p 3306:3306 -e MYSQL_ROOT_PASSWORD=123456 -e MYSQL_DATABASE=duagent mysql:8
docker run -d --name eduagent-qdrant -p 6333:6333 -p 6334:6334 qdrant/qdrant
```

Agent Service：

```bash
cd /home/yezisama/workspace/workflow/EDUagent/agent_service
./.venv/bin/uvicorn agent_service.main:app --host 127.0.0.1 --port 8002
```

Backend：

```bash
cd /home/yezisama/workspace/workflow/EDUagent/backend
source ../.venv/bin/activate
python -m uvicorn app.main:app --host 127.0.0.1 --port 8001 --reload
```

### 4.2 健康检查

必须执行：

```bash
curl http://127.0.0.1:8001/health
curl http://127.0.0.1:8002/agent/v1/health
curl http://127.0.0.1:6333/healthz
```

通过标准：

- Backend 返回 `{"status":"ok","version":"5.0"}`
- Agent 返回 `code=200`，且 `data.status=healthy`
- Agent 返回 `data.qdrant_connected=true`
- Qdrant `/healthz` 返回健康结果

### 4.3 环境一致性检查

重点核对：

- Backend `AGENT_SERVICE_URL` 指向 `http://127.0.0.1:8002`
- Backend 与 Agent 的 `WEBHOOK_SECRET` 一致
- MySQL 可连通
- Qdrant 可连通

若要验证资源或 RAG 链路，还应确认：

- Agent 课程知识已灌入 Qdrant
- 当前测试课程 ID 与已灌入课程 ID 一致

当前已知可用课程：

- `course_id = 758aeff588e84044`
- 已在 Qdrant 中摄入约 `760` 个课程知识 chunk

## 5. AI 执行规则

后续 AI 按本文执行时，必须遵守：

1. 先做前置检查，再开始业务验收。
2. 每一阶段都要记录：
   - 账号角色
   - `course_id`
   - `task_id`
   - 终态
   - 关键日志证据
3. 不能只看 HTTP 响应；异步链路必须同时看：
   - `GET /tasks/{task_id}`
   - 数据是否落库
   - Agent 日志
4. 对 RAG 型接口，必须额外看：
   - embeddings 调用成功
   - Qdrant 查询成功
   - 存在命中信号
5. 若某接口设计上不走 RAG，不得强行要求 embeddings/Qdrant 日志。
6. 若结果为空，先判断是“前置数据缺失导致的合理降级”，还是“链路故障”。
7. 所有结论要带绝对事实，不用模糊表述。

## 6. 测试环境变量建议

建议在 Apifox 或脚本环境中固定以下变量：

- `baseUrl = http://127.0.0.1:8001`
- `agentBaseUrl = http://127.0.0.1:8002`
- `teacher_token =`
- `student_token =`
- `course_id =`
- `course_code =`
- `task_id =`
- `conversation_id =`

建议把本轮测试账号命名成显式用途，例如：

- `teacher_rag_test_01@test.com`
- `student_rag_test_01@test.com`

## 7. 阶段化验收流程

以下流程按推荐顺序执行。除非只做局部回归，否则不要跳步。

### Phase 0：上下文记录

先记录：

- 当前日期时间
- 当前分支
- Backend / Agent Service 是否正在运行
- 当前使用的 `course_id`
- 本轮计划验收的接口范围

### Phase 1：账号与课程 seed

目标：获得一对可复用的教师/学生账号和一门课程。

步骤：

1. `GET /api/v1/auth/captcha`
2. 教师注册
3. 学生注册
4. 教师登录
5. 学生登录
6. 教师创建课程
7. 学生加入课程
8. `GET /api/v1/courses` 验证课程对对应角色可见

通过标准：

- 教师登录成功并获得 `teacher_token`
- 学生登录成功并获得 `student_token`
- 课程创建成功并获得 `course_id`、`course_code`
- 学生加课成功

失败定位：

- 注册失败：优先看 captcha / registration_code
- 登录失败：优先看账号重复、验证码过期
- 加课失败：优先看课程码是否使用了旧值

### Phase 2：基础异步任务闭环

目标：确认 Backend 的真异步任务框架可用。

推荐先验：

- `POST /api/v1/profile/refresh?course_id=...`
- `POST /api/v1/evaluation/refresh?course_id=...`

每条链路都要验证：

1. 请求返回 `202 + task_id`
2. 立即 `GET /api/v1/tasks/{task_id}` 为 `processing`
3. 轮询后变成 `completed` 或 `failed`
4. 对应业务表有新记录或明确失败原因

通过标准：

- task 状态闭环存在
- 没有永远停留在 `processing`
- 成功时真实写库
- 失败时有 `error_code/error_message`

### Phase 3：resources + webhook 真实闭环

目标：验证 `Backend -> Agent -> Webhook -> SQL` 的跨服务异步链路。

执行步骤：

1. 教师调用 `POST /api/v1/resources/generate`
2. 记录返回的 `task_id`
3. 立即查 `GET /api/v1/tasks/{task_id}`，应为 `processing`
4. 轮询至 `completed` 或 `failed`
5. 查询 `GET /api/v1/resources?course_id=...`
6. 同时观察 Agent 日志是否发起 webhook

真实通过标准：

- `202 + task_id`
- task 从 `processing` 进入 `completed`
- `task.result.resources` 存在资源数组或等价结果
- `resources` 表有真实新记录
- Agent 日志显示已向 Backend webhook 回调

失败定位：

- Backend 到 Agent 调用失败：看 Backend 日志
- Agent 处理成功但 task 不结束：看 webhook 是否到达
- webhook 到达但不落库：看 Backend webhook 日志、鉴权 header、payload 结构

备注：

- 该链路已完成过真实联调验收，当前可作为高优先复验项

### Phase 4：tutoring/chat 真实 RAG 验收

目标：验证真实课程知识检索是否参与回答。

这是当前最重要的 RAG 验收链路。

执行前提：

- 测试课程在 Qdrant 中已有知识数据
- `scope=course` 且传入有效 `course_id`

执行步骤：

1. 学生调用 `POST /api/v1/tutoring/chat`
2. 请求体中使用与课程内容强相关的问题
3. 观察 Backend 是否返回 `text/event-stream`
4. 观察 Agent 日志中的检索信号
5. 检查最终回答是否体现课程知识，而不是泛化常识回答

推荐问题示例：

- “请解释顺序表为什么支持随机访问？”
- “链表和顺序表在插入删除上的差异是什么？”
- “栈和队列的核心区别是什么？”

真实通过标准：

- Backend 返回 SSE，不是普通 JSON
- 响应流中能看到 `chunk` 和最终 `done`
- Agent 日志出现 embeddings 成功
- Agent 日志出现 Qdrant 查询成功
- Agent 日志存在检索命中信号，例如命中数量、检索片段、RAG hit count
- 回答内容与课程知识一致

降级可用标准：

- SSE 正常返回
- 但没有检索命中，回答仍可读

未通过标准：

- 不是 SSE
- Agent 未收到请求
- embeddings/Qdrant 报错且回答退化严重

### Phase 5：quiz/generate + submit + result

目标：验证个性化出题和答题结果链路。

执行步骤：

1. 学生调用 `POST /api/v1/quiz/generate`
2. 记录 `task_id`
3. 轮询任务至 `completed`
4. 查询 `GET /api/v1/quiz/questions`
5. 选取题目后调用 `POST /api/v1/quiz/submit`
6. 查询 `GET /api/v1/quiz/result`

真实通过标准：

- `quiz/generate` 返回 `202 + task_id`
- 题目真实落库
- `submit` 返回正确评分结果
- `result` 能返回课程级统计

补充事实：

- `quiz` 集成测试当前已补至 `71/71` 通过
- `submit/result` 仍保持半完成，因为 diagnosis 契约语义仍待收口

失败定位：

- 无题目落库：看 Agent 出题返回和 Backend 写库
- 评分异常：看 question_id、答案结构、非法题目容错
- result 统计异常：看历史 session / answer 数据

### Phase 6：learning-path/refresh 推荐链路

目标：验证学习路径刷新链路本身是否工作，并区分“链路通过”和“内容为空”的原因。

关键事实：

- 该接口不走 Qdrant RAG
- 它依赖 Backend 提供：
  - `CourseKnowledgeGraph`
  - `UserProfile`
  - `Evaluation`
- 当前若缺 `CourseKnowledgeGraph`，返回空 `nodes/edges` 是合理结果

执行步骤：

1. 确保学生已加入课程
2. 调用 `POST /api/v1/learning-path/refresh?course_id=...`
3. 记录 `task_id`
4. 轮询任务至终态
5. 查询 `GET /api/v1/learning-path?course_id=...`
6. 判断结果为空的原因

真实通过标准：

- `202 + task_id`
- task `processing -> completed`
- `learning_paths` 表有新记录
- 若课程已有 `CourseKnowledgeGraph`，则 `nodes/edges` 应非空

降级可用标准：

- 任务闭环正确
- LearningPath 行已写入
- 但 `nodes/edges` 为空，且确认是因为没有 `CourseKnowledgeGraph`

错误判定示例：

- 不能因为没有 embeddings/Qdrant 日志就判失败
- 不能因为 `nodes=0` 就直接判 Agent 故障

### Phase 7：结果汇总

本轮联调结束后，至少输出以下摘要：

- 验收日期
- 使用的 `course_id`
- 验收的接口列表
- 每条链路结论：真实通过 / 降级可用 / 未通过
- 关键证据
- 已知 gap
- 下一步建议

## 8. 数据与前置条件矩阵

为避免误判，执行前先看清楚每条链路依赖什么：

| 链路 | 是否异步 | 是否 RAG | 是否依赖 Webhook | 关键前置数据 |
|------|----------|----------|------------------|--------------|
| `profile/refresh` | 是 | 否 | 否 | 学生已入课 |
| `evaluation/refresh` | 是 | 否 | 否 | 学生已入课 |
| `learning-path/refresh` | 是 | 否 | 否 | 学生已入课，最好有 KG/profile/evaluation |
| `resources/generate` | 是 | 可带课程知识上下文 | 是 | 教师 token，webhook secret 一致 |
| `tutoring/chat` | 否，SSE | 是 | 否 | 课程知识已灌入 Qdrant |
| `quiz/generate` | 是 | 可带课程上下文 | 否 | 学生已入课 |
| `quiz/submit` | 否 | 后台异步 diagnosis | 否 | 已有 quiz 题目 |

## 9. 常见失败定位

### 9.1 Backend 启动失败

优先检查：

- MySQL 是否启动
- 数据库连接串是否正确
- 表是否存在

### 9.2 Agent 健康但业务失败

优先检查：

- 调用的 `course_id` 是否与实际测试数据一致
- Agent 日志是否报模型、embedding、webhook 错误
- 请求体是否缺必要字段

### 9.3 task 一直 `processing`

优先检查：

- Backend 后台协程是否实际启动
- Agent 是否返回异常
- 是否因进程 reload/重启导致 `asyncio.create_task` 丢失

### 9.4 resources 已生成但 Backend 看不到结果

优先检查：

- Webhook 是否发出
- `X-Webhook-Secret` 是否一致
- `task_id` 是否匹配
- webhook payload 是否包含预期 `status/result/error_code`

### 9.5 tutoring 有回答但不像课程知识回答

优先检查：

- `scope=course` 是否带了 `course_id`
- 问题是否与课程知识确实相关
- Agent 日志中是否有 embeddings 和 Qdrant 命中
- 是否误用了未灌入知识的课程 ID

### 9.6 learning-path 空路径

优先检查：

- `course_knowledge_graphs` 是否有该课程数据
- 学生是否已加课
- profile/evaluation 是否存在

如果确认 KG 为空，则结论应为“降级可用”，不是“实现故障”。

## 10. 当前已知 gap

截至当前阶段，以下问题已知但不应与联调故障混淆：

1. `learning-path/refresh` 缺正式的 `CourseKnowledgeGraph` 生成/导入链路。
2. `profile/evaluation/learning-path refresh` 仍是进程内 `asyncio.create_task`，不具备持久化恢复能力。
3. `quiz result` 的 diagnosis 语义仍待收口；当前课程级结果与后台 `diagnosis_json` 并存。

## 11. 推荐的最小复验集

若时间有限，至少执行以下 5 项：

1. `GET /health`
2. `GET /agent/v1/health`
3. `POST /api/v1/resources/generate` + task 终态 + 资源落库
4. `POST /api/v1/tutoring/chat` + SSE + RAG 日志
5. `POST /api/v1/learning-path/refresh` + 判定空结果是否由缺 KG 导致

这 5 项足以快速判断：

- 两个服务是否存活
- 异步任务与 webhook 是否闭环
- RAG 是否真实生效
- 推荐链路是否因前置数据缺失而降级

## 12. 建议的输出模板

每次联调结束，可按以下模板记录：

```text
联调日期：
测试课程：
教师账号：
学生账号：

Phase 1 Seed：
- 结果：

Phase 2 Refresh：
- profile/refresh：
- evaluation/refresh：

Phase 3 Resources：
- task_id：
- 终态：
- 落库：

Phase 4 Tutoring：
- SSE：
- embeddings：
- Qdrant：
- 命中：

Phase 5 Quiz：
- generate：
- submit：
- result：

Phase 6 Learning Path：
- task_id：
- 终态：
- nodes/edges：
- 结论：真实通过 / 降级可用 / 未通过

已知 gap：
下一步建议：
```
